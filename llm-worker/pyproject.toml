[project]
name = "llm-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain>=0.3",
    "langchain-core>=0.3",
    "langchain-openai>=0.2",
    "confluent-kafka>=2.5",
    "qdrant-client[fastembed]>=1.12",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "loguru>=0.7",
    "python-dotenv>=1.0",
    "minio>=7.2",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8",
    "pytest-mock>=3",
    "ruff>=0.7",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
